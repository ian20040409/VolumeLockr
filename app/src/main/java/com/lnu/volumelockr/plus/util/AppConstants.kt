package com.lnu.volumelockr.plus.util

object AppConstants {
    // Broadcast Actions
    const val ACTION_SET_LOCK = "com.lnu.volumelockr.plus.ACTION_SET_LOCK"
    const val ACTION_PAIRED_SUCCESS = "com.lnu.volumelockr.plus.ACTION_PAIRED_SUCCESS"

    // Intent Extras
    const val EXTRA_STREAM = "stream"
    const val EXTRA_VOLUME = "volume"
    const val EXTRA_LOCKED = "locked"
    const val EXTRA_UPDATE_IF_LOCKED_ONLY = "update_if_locked_only"
}
